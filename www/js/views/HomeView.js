"use strict";

define(["backbone", "jquery"], function(Backbone, $) {

  var template = _.template([

    ]),
    hints = [
      "Must be B, C, P or U",
      "Must be 0 or 1",
      "Can be any number"
    ];

});
